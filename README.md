# REST-framework-API-with-API-integration

Uses Django's REST framework to create an API for a hospital system and integrates the the API into another application.

Taskmanager is the application.
Patients is the API
